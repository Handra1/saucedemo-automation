{
  "name": "saucedemo-automation",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
  "test": "mocha tests --recursive --timeout 60000",
  "report:mochawesome": "mocha tests --recursive --timeout 60000 --reporter mochawesome",
  "report:simple": "mocha tests --recursive --timeout 60000 --reporter mocha-simple-html-reporter --reporter-options output=report.html"
},
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "mocha": "^11.7.0",
    "mocha-simple-html-reporter": "^2.0.0",
    "mochawesome": "^7.1.3",
    "selenium-webdriver": "^4.33.0"
  }
}
